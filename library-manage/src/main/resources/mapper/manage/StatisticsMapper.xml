<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lz.manage.mapper.StatisticsMapper">
    <select id="appointmentStatistics" resultType="com.lz.manage.model.statistics.po.StatisticsPo">
        select count(*) as value,DATE_FORMAT(create_time, '%Y-%m') as name
        from tb_appointment_info
        <where>
        <if test="startTime != null">
            and create_time >= #{startTime}
        </if>
        <if test="endTime != null">
            and create_time &lt; #{endTime}
        </if>
        </where>
        group by DATE_FORMAT(create_time, '%Y-%m')
        order by DATE_FORMAT(create_time, '%Y-%m')
    </select>

    <resultMap id="AppointmentInfoMap" type="com.lz.manage.model.domain.AppointmentInfo">
        <id column="id" property="id"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
    </resultMap>

    <select id="appointmentHourStatistics" resultMap="AppointmentInfoMap">
        select id, start_time, end_time
        from tb_appointment_info
        <where>
            <if test="startTime != null">
                and create_time >= #{startTime}
            </if>
            <if test="endTime != null">
                and create_time &lt; #{endTime}
            </if>
        </where>
    </select>
</mapper>
